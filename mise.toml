# Dev toolchain, managed by mise (https://mise.jdx.dev).
# Nub (https://nubjs.com) is the installer and script runner.
# `mise install` provisions Node and Nub.
[tools]
node = "24"
"npm:@nubjs/nub" = "latest"
